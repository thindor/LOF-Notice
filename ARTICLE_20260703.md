# LOF套利系统：一次核心溢价计算的修正

> 做了一次交叉验证后发现，系统筛选出来的"溢价套利机会"和实际申购能拿到的价差根本对不上。排查下来，问题出在溢价计算的分母上。

---

## 怎么发现的

系统跑了一个多月，每天都能筛出几只溢价 >3% 的品种。但回过头看模拟交易的盈亏，发现一个规律：**买入时的溢价和 T+2 卖出后的实际收益几乎没有相关性。** 按理说溢价 3% 买入，扣除 0.16% 费用，T+2 卖出至少应该有 2% 左右的利润，但实际盈亏经常在 ±1% 之间随机波动。

这说明"溢价率"这个数字本身有问题——它没有反映真实的买入成本。

于是做了一次交叉验证：拿同一批 LOF，用自己的系统和 lof8.cn 的公开 API 分别算溢价率，对比差异。

结果很有意思：对于大多数品种，两边的数值差了一个数量级。自己算出来 +3.74% 的品种，lof8 显示只有 +0.57%。差距不是误差，是算法不同——自己用的是昨天净值，lof8 用的是盘中实时估算净值。

---

## 问题：溢价算的是昨天的净值，申购执行的是今天的净值

LOF 溢价套利的基本逻辑是：场内价格高于净值时，按净值申购，T+2 到场内按市价卖出赚差价。

但这里有一个时间差：申购按**当天收盘后的净值**成交，而系统之前计算溢价用的是**昨天已公布的净值**。如果当日市场有明显波动，这两个净值之间的差距会把溢价吃掉。

具体来说，之前的代码是这样的：

```python
# fetcher.py verify_navs_batch —— 之前的逻辑
verified_premium = (price - yesterday_nav) / yesterday_nav
```

`yesterday_nav` 是天天基金返回的 `dwjz`（最新已公布净值），通常是 T-1 或 T-2 的数据。

以 7 月 3 日的数据为例，某只 LOF：

- 昨天净值（dwjz）= 3.986
- 盘中场内价 = 4.135
- 修正前显示溢价 = (4.135 - 3.986) / 3.986 = **+3.74%**

但当天市场上涨，天天基金的盘中估算净值（est_nav，基于持仓成分股实时涨跌）是 4.1114。真实溢价：

- (4.135 - 4.1114) / 4.1114 = **+0.57%**

3.74% 和 0.57%，差了 6 倍。修正前看起来有套利空间，修正后刚够覆盖手续费。

est_nav 这个数据一直有——天天基金 API 的 `gsz` 字段返回的就是它，代码也存到了 `item["est_nav"]` 里，只是之前没有用于溢价计算。

---

## 修改内容

### 1. 溢价计算改用盘中估算净值（est_nav）

**文件：** `fetcher.py`

- 新增 `premium_rt_est` 字段：`(场内价 - est_nav) / est_nav`
- `filter_arbitrage_opportunities` 主筛选改用实时溢价，优先 `premium_rt_est`，无 est_nav 时降级使用昨天的 `premium_rt_verified`
- 同时保留 `premium_rt_verified`（昨天净值溢价）作为参考对比

```python
# 修正后
est_premium = (price - est_nav) / est_nav   # ★ 这才是真实溢价
```

### 2. 模拟买入改用 est_nav 估算成本

**文件：** `trade_tracker.py`

- `auto_buy_opportunities` 的买入净值从 `nav_verified`（昨天净值）改为 `est_nav`
- 因为申购成本是当天收盘净值，est_nav 是最接近的估算

### 3. 成交额阈值修正

**文件：** `trade_tracker.py`

- `auto_buy_opportunities` 的成交额门槛从 1 万元修正为 1000 万元
- 与 `fetcher.py` 的 `MIN_VOLUME` 保持一致

### 4. 接入 lof8.cn 作为交叉验证数据源

**文件：** `fetcher.py`

- 新增 `fetch_lof8_status()` 调用 `https://lof8.cn/lof-monitor/api/lof`（公开 API）
- 新增 `enrich_with_lof8()` 做两件事：
  - 用 lof8 的实时申购状态覆盖过期数据（替代 14 天未更新的集思录缓存）
  - 添加 `lof8_premium` 字段，两套独立系统溢价率交叉验证
- 新增 `_parse_lof8_status()` 解析申购状态字符串，提取具体限额

---

## 效果

修正前后对比（7 月 3 日盘中数据）：

| | 修正前 | 修正后 |
|------|--------|--------|
| 溢价 >3% 且开放申购 | 3 只 | **0 只** |
| 溢价计算基准 | 昨天净值 | 实时估算净值 |
| 买入成本估算 | 昨天净值 | 实时估算净值 |

交叉验证：lof8.cn 对同一只基金显示溢价 +0.57%，修正后系统显示 +0.57%，两套独立系统结果一致。

---

**总结：将溢价计算的分母从"昨天已公布净值"改为"盘中实时估算净值"，消除了估值滞后导致的虚高溢价。**