# 一个读者告诉我，我的LOF套利系统"全算错了"

> 这篇文章记录了一次尴尬但价值巨大的代码审查——一位读者指出我的溢价套利逻辑有致命缺陷，而我用了半天时间验证他说的是对的。

---

## 起因：一条读者反馈

几天前收到了一个反馈，大意是：

> "你这个LOF申购是乱来的。很多申购价根本买不到，全网就你这里瞎搞。"

说实话，第一反应是不服气——系统跑了一个多月，数据、筛选、自动买卖都正常运转，怎么会是"瞎搞"？

但冷静下来想：如果一个真做过LOF套利的人觉得有问题，大概率不是空穴来风。于是打开代码，从第一性原理重新推了一遍整个套利逻辑链。

---

## LOF溢价套利的真实时间线

LOF套利的原理看起来很简单：

1. **T日 14:40**：看到场内价格高、净值低 → 按净值申购
2. **T+2日**：份额到场内 → 按场内价卖出 → 赚差价

但在时间轴上展开之后，问题就出来了：

```
T日 14:40           T日 15:00收盘      T日晚上             T+2日 9:30
   ↓                    ↓                  ↓                    ↓
你看到的             申购截止            真实净值公布          你能卖出的价格
"溢价"              你的实际成本         才发现              可能完全变了
是估算              是当天收盘净值       溢价有多少
```

**关键矛盾**：你在 14:40 看到的"溢价率"，分子是当下的场内价格，分母是昨天的净值。但你的真实成本——申购执行价——是今天收盘后的净值。而你在申购的时候根本不知道这个数。

---

## 找到Bug：用昨天的尺子量今天的身高

翻到 `fetcher.py` 的净值核实逻辑，看到这行：

```python
verified_premium = (price - yesterday_nav) / yesterday_nav  # ← 分母是昨天的净值
```

这就是问题。举个实际例子：

今天是 7月3日，某只LOF：
- 昨天净值（dwjz）= 3.986
- 今天盘中场内价 = 4.135
- 系统算的溢价 = (4.135 - 3.986) / 3.986 = **+3.74%** ← 看起来有套利空间！

但今天相关股票涨了。天天基金根据持仓成分股实时涨跌算出的今天估算净值（est_nav）是 4.1114。真实溢价：

- (4.135 - 4.1114) / 4.1114 = **+0.57%** ← 刚够覆盖手续费

3.74% → 0.57%，差了 6 倍。套利空间几乎蒸发。

**更讽刺的是**：`est_nav` 这个字段代码里明明拿到了（天天基金 API 返回的 `gsz`），存在 `item["est_nav"]` 里，然后——再也没用过。属于拿了答案不看的典型。

---

## 修了三件事

### 1. 溢价计算改用 est_nav（核心）

```python
# 之前
premium = (price - yesterday_nav) / yesterday_nav

# 之后
premium = (price - est_nav) / est_nav  # est_nav = 盘中实时估算净值
```

### 2. 买入成本估算同步修正

`trade_tracker.py` 里模拟买入用的净值也从昨天净值改成了 est_nav。因为申购真实成本就是今天的收盘净值，est_nav 是最接近的估算。

### 3. 成交额阈值修了个1000倍的Bug

```python
# 之前（trade_tracker.py）
if amount < 10_000:      # 1万元 → 流动性枯竭的品种也会被买入
    continue

# 之后
if amount < 10_000_000:  # 1000万 → 与筛选条件保持一致
    continue
```

这之前因为上游过滤更严所以没触发，但万一哪天数据流改了就是定时炸弹。

---

## 接入 lof8.cn 做交叉验证

修完之后，需要验证新的溢价计算是否正确。调研了几个外部数据源：

| 网站 | 溢价算法 | 数据量 | 可用性 |
|------|----------|--------|--------|
| haoetf.com/lof | 实时估值 ✅ | 1只（测试中）| ❌ |
| wanka5.com | 昨天净值 ❌ | 22只QDII | ❌ |
| **lof8.cn** | **实时估值 ✅** | **80只** | **✅ 公开API** |

lof8.cn 有个公开 API（`/lof-monitor/api/lof`），免费、不需要登录、返回 80 只 LOF 的实时估值溢价。而且它的申购状态比我们集思录的 14 天缓存要新得多——能精确到"暂停申购(限10元)"、"限购50万"这种粒度。

接入后做了一次交叉验证：lof8 显示同一只基金溢价 +0.57%，我们显示 +0.57%。两套独立系统结果一致，说明 est_nav 修正方向没问题。

---

## 修复后的第一个结论：今天一个能买的都没有

修复前，系统每天能筛出 3-5 只"溢价>3%且开放申购"的品种。

修复后，7月3日 11:57 的数据跑下来：

```
total=0, count=0
```

**零只。**

不是系统坏了。是之前的"机会"大部分是估值滞后的幻觉。当天市场涨了，净值还没更新，溢价看起来高。用实时估值一算，全在 3% 以下甚至变折价。

---

## 诚实地说

这个 Bug 暴露了两个更底层的问题：

**第一，我们缺少 T+2 溢价衰减数据。** 即使 est_nav 把 T 日的溢价算准了，T+1、T+2 两天里溢价会被套利盘打下来多少？这个衰减率我们完全没数据。需要积累至少 2-4 周的历史序列。

**第二，QDII LOF 的 est_nav 可能也不准。** 天天基金只披露前十大重仓股，海外基金调仓频繁，时差又导致净值延迟更久。QDII 套利的不确定性比本土 LOF 大得多。

但目前至少第一步是对的：**不再用昨天的净值赌后天的价格。**

---

## 修改清单

| 文件 | 改动 |
|------|------|
| `fetcher.py` | 新增 `premium_rt_est` 字段（基于实时估值）、筛选改用 est_nav 溢价、接入 lof8.cn API |
| `trade_tracker.py` | 买入决策和成本估算改用 est_nav、成交额阈值 1万→1000万 |
| `main.py` | 配合新字段输出 |

---

*一条金句收尾：*

**"套利工具不骗人，是套利工具用错了数，骗了自己。"**